<div class="container">
    <h2>Cancel Booking</h2>

    {{#if booking}}
        <div class="booking-details">
            <h3>Booking Details</h3>
            <table>
                <tr>
                    <td><strong>Date & Time:</strong></td>
                    <td>{{formatSlot booking.slot}}</td>
                </tr>
                <tr>
                    <td><strong>Event:</strong></td>
                    <td>{{booking.eventName}}</td>
                </tr>
            </table>
        </div>

        <div class="cancel-section">
            <p class="warning">Are you sure you want to cancel this booking? This action cannot be undone.</p>

            <form method="POST" action="/cancel-booking/{{booking.timeslotId}}">
                <div class="btn-group">
                    <button type="submit" class="btn btn-danger">Yes, Cancel Booking</button>
                    <a href="/my-bookings" class="btn">No, Keep Booking</a>
                </div>
            </form>
        </div>
    {{else}}
        <div class="error">
            <p>Booking not found, already cancelled, or you don't have permission to cancel it.</p>
            <a href="/my-bookings" class="btn">Back to My Bookings</a>
        </div>
    {{/if}}
</div>
