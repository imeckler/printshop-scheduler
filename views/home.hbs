<div class="container">
    {{#if user}}
        <h2>Welcome back, {{user.name}}!</h2>
        
        <div class="btn-group">
            <a href="/book" class="btn btn-primary">Book a Sauna Slot</a>
            <a href="/my-bookings" class="btn">View My Bookings</a>
        </div>
        
        {{#if upcomingBookings}}
            <h3>Your Upcoming Bookings</h3>
            <table>
                <thead>
                    <tr>
                        <th>Date & Time</th>
                        <th>Unit</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each upcomingBookings}}
                    <tr>
                        <td data-label="Date & Time">{{formatDate this.slot}}</td>
                        <td data-label="Unit">{{this.unit.name}}</td>
                        <td data-label="Status">{{this.status}}</td>
                        <td data-label="Actions">
                            <a href="/cancel-booking/{{this.bookingId}}" class="btn" onclick="return confirm('Are you sure you want to cancel this booking?')">Cancel</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        {{else}}
            <p>You don't have any upcoming bookings. <a href="/book">Book your first sauna session!</a></p>
        {{/if}}
    {{else}}
        <h2>Relax and Unwind</h2>
        <p>Welcome to our premium sauna booking system. Enjoy a peaceful and rejuvenating experience in our state-of-the-art sauna facilities.</p>
        
        <h3>Features:</h3>
        <ul>
            <li>Multiple sauna units with different capacities</li>
            <li>Easy online booking system</li>
            <li>Flexible scheduling</li>
            <li>Secure phone number verification</li>
        </ul>
        
        <div class="btn-group">
            <a href="/login" class="btn btn-primary">Login to Book</a>
        </div>
    {{/if}}
</div>